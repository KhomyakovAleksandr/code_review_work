name: Build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt install -y openjdk-17-jdk
      - run: javac *.java 2>/dev/null || echo "Compiling..."
